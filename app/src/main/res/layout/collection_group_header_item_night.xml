<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/collection_group_first_word"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:layout_centerHorizontal="true"
        android:textSize="28sp"
        android:textColor="@color/red_text_bg"
        android:fontFamily="@font/font_family"
        android:text="A" />

    <TextView
        android:id="@+id/collection_group_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/collection_group_first_word"
        android:gravity="center"
        android:textSize="@dimen/sp_16"
        android:fontFamily="@font/font_family"
        android:textColor="@color/red_text_bg"
        android:text="@string/app_name" />

    <View
        android:layout_width="2dp"
        android:layout_height="40dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/collection_group_title"
        android:background="@color/red_text_bg" />

</RelativeLayout>